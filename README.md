# Proactive Security Patch Automation Framework 🛡

## Setup

### Backend (Colab / Local)
```bash
cd backend
pip install -r requirements.txt
python backend.py
cd frontend
pip install -r requirements.txt
streamlit run app.py
